# Partner logos

One logo per partner or sponsor, named after the partner's content slug. Prefer SVG; use
PNG with a transparent background otherwise.

Not for SPADES' own logo — that lives in `../../marks/`.

Example: `lums-oss.svg`
