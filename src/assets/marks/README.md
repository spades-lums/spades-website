# src/assets/marks

SPADES' own identity: the logo mark, the wordmark, and the hand-drawn SVG marks
(underlines, circles, arrows) used as accents in the design.

Not for partner logos (`../logos/partners/`) or photos (`../images/`).

Example: `underline-squiggle.svg`
