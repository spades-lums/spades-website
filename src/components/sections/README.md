# components/sections

Big page blocks, one folder per page type: the hero on the homepage, the roster block on a
team page. Sections may read content and compose `../ui/` pieces.

Not for generic reusable widgets — those go in `../ui/`.

Example: `home/Hero.astro`
