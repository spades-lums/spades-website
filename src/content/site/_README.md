# content/site

Site-wide settings and text that is not a page: contact email, social links, footer text,
the navigation menu.

Not for page content — that goes in `../pages/` or a specific collection.

Example: `settings.yaml`
