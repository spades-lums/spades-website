# content/flagships

One file per recurring flagship event — PsiFi, LSS, Startup Weekend — describing the event
in general, across all years.

Not for a specific year's running of it; that is an "edition" in `../editions/`.

Example: `psifi.md` → `/psifi`
