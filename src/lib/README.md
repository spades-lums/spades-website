# src/lib

Plain TypeScript helpers with no UI and no React: date formatting, slug helpers, sorting
content entries.

Not for React hooks (`../hooks/`) or anything that renders. Keep each file small and
exporting named functions.

Example: `format-date.ts`
